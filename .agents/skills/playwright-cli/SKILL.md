---
name: playwright-cli
description: Use Playwright CLI for browser automation, test generation and debugging, tracing, request mocking, storage state, sessions, video recording, and element inspection.
---

# Playwright CLI

Before performing a Playwright CLI task, read and follow the official skill at
`../../../.claude/skills/playwright-cli/SKILL.md` completely. Resolve that skill's relative references from its own
directory.
